[project]
name = "giljob-local-infer"
version = "0.1.0"
description = "Low-latency Gemma4 video-native inference gateway for GilJob"
requires-python = ">=3.11"
dependencies = [
  "fastapi",
  "uvicorn",
  "requests",
  "pydantic",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
